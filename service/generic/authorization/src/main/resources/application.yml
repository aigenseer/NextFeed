spring:
  config:
    import:
      - classpath:nextfeed.config.yml
      - classpath:security.config.yml
  application:
    name: authorization-service
  main:
    allow-bean-definition-overriding: true

server:
  port: ${nextfeed.service.authorization-service.port}

