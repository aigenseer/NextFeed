server:
  port: 8230
#  port: 0

spring:
  config:
    import:
      - classpath:eureka.client.config.yml
  application:
    name: session-socket-service
  main:
    allow-bean-definition-overriding: true

nextfeed:
  socket:
    endpoint:
      prefix: "/socket/session-socket"

