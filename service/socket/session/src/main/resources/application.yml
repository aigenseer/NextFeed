spring:
  config:
    import:
      - classpath:nextfeed.config.yml
  application:
    name: session-socket-service
  main:
    allow-bean-definition-overriding: true

server:
  port: ${nextfeed.service.session-socket-service.port}

nextfeed:
  socket:
    endpoint:
      prefix: "/socket/session-socket"

