spring:
  config:
    import:
      - classpath:nextfeed.config.yml
      - classpath:security.config.yml
  application:
    name: survey-service
  main:
    allow-bean-definition-overriding: true

server:
  port: ${nextfeed.service.survey-service.port}
