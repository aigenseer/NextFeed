spring:
  config:
    import:
      - classpath:nextfeed.config.yml
      - classpath:security.config.yml
  application:
    name: question-service
  main:
    allow-bean-definition-overriding: true

server:
  port: ${nextfeed.service.question-service.port}
