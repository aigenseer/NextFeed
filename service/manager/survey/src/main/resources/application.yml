spring:
  config:
    import:
      - classpath:nextfeed.config.yml
  application:
    name: survey-manager-service
  main:
    allow-bean-definition-overriding: true

server:
  port: ${nextfeed.service.survey-manager-service.port}
