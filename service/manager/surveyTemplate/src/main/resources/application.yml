server:
  port: 8160

spring:
  config:
    import:
      - classpath:db.config.yml
      - classpath:eureka.client.config.yml
  application:
    name: survey-template-manager-service
  main:
    allow-bean-definition-overriding: true
