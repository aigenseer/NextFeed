spring:
  config:
    import:
      - classpath:nextfeed.config.yml
  application:
    name: system-manager-service
  main:
    allow-bean-definition-overriding: true

server:
  port: ${nextfeed.service.system-manager-service.port}
