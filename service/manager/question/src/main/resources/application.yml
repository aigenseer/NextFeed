spring:
  config:
    import:
      - classpath:nextfeed.config.yml
      - classpath:db.config.yml
  application:
    name: question-manager-service
  main:
    allow-bean-definition-overriding: true

server:
  port: ${nextfeed.service.question-manager-service.port}