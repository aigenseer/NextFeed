server:
  port: 8130

spring:
  config:
    import:
      - classpath:db.config.yml
      - classpath:eureka.client.config.yml
  application:
    name: question-manager-service
  main:
    allow-bean-definition-overriding: true
