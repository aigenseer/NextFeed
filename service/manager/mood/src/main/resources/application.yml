spring:
  config:
    import:
      - classpath:nextfeed.config.yml
  application:
    name: mood-manager-service
  main:
    allow-bean-definition-overriding: true

server:
  port: ${nextfeed.service.mood-manager-service.port}

grpc:
  server:
    port: ${nextfeed.service.mood-manager-service.grpc-port}
