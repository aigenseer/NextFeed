spring:
  config:
    import:
      - classpath:nextfeed.config.yml
  application:
    name: user-manager-service
  main:
    allow-bean-definition-overriding: true

server:
  port: ${nextfeed.service.user-manager-service.port}

grpc:
  server:
    port: ${nextfeed.service.user-manager-service.grpc-port}



