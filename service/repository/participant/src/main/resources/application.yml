spring:
  config:
    import:
      - classpath:nextfeed.config.yml
      - classpath:db.config.yml
  application:
    name: participant-repository-service
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:postgresql://localhost:5412/nextfeed

server:
  port: ${nextfeed.service.participant-repository-service.port}
